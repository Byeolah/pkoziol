{% include 'default/_form.html.twig' with {
    page_title: 'title.calendar_edit',
    page_title_param: {'%id%': calendar.id},
    attr: {
        method: 'PUT',
        action: url('calendar_edit', {'id': calendar.id}),
    },
    back_to_list_path: 'calendar_index',
} %}