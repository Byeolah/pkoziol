{% include 'default/_form.html.twig' with {
    page_title: 'title.role_change',
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'PUT',
        action: url('role_edit', {'id': user.id}),
    },
    back_to_list_path: 'admin_user_index',
} %}