{% include 'default/_form.html.twig' with {
    page_title: 'action.edit_password',
    attr: {
        method: 'PUT',
        action: url('password_edit', {'id': profile.id}),
    },
    back_to_list_path: 'task_index'
} %}