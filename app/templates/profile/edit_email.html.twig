{% include 'default/_form.html.twig' with {
    page_title: 'action.edit_email',
    attr: {
        method: 'PUT',
        action: url('email_edit', {'id': app.user.id}),
    },
    back_to_list_path: 'task_index'
} %}