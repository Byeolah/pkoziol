{% include 'default/_form.html.twig' with {
    page_title: 'title.profile_delete',
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'DELETE',
        action: url('profile_delete', {'id': user.id}),
    },
    back_to_list_path: 'bookmark_index',
} %}